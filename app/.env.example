# Set your camera RTSP URL (replace with your credentials and IP)
# Example for TP-Link Tapo (stream1 = main, stream2 = sub):
# RTSP_URL=rtsp://<USER>:<PASSWORD>@<CAM_IP>:554/stream1
RTSP_URL=rtsp://user:password@192.168.1.123:554/stream1

# Optionally change the default path name in the player (keep in sync with mediamtx.yml)
# STREAM_PATH=cam

# Gemini settings for the gateway (image analysis)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash-latest
